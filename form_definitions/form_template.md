# } —}

- **약칭**: {{short_name}}
- **수신처**: (株)農心ジャパン
- **메모**: {{memo}}
- **특이사항**: `<!-- 예: 한 PDF = 단일 請求書 / 한 PDF에 복수 請求書 묶임 -->`

## 식별 패턴

<!-- 이 양식을 다른 양식과 구별하는 OCR 출력 내 고유 컬럼명·키워드 조합.
     Phase 1 결과에서 이 패턴이 확인되면 해당 양식으로 판별.
     예: `得意先名` · `商品名` · `消費税率`
          `得意先又は商品` · `未収条件` -->

`{고유 컬럼명 또는 키워드}` · `{고유 컬럼명 또는 키워드}`

---

## 문서 구조

<!-- PDF 전체 구성을 코드 블록으로 도식화.
     예: cover → detail × N → summary 순서, 또는 請求書 단위 반복 구조 -->

```
PDF p.1    — cover
PDF p.2〜  — detail
PDF p.N    — summary (있으면)
```

---

<!-- ▼ 선택 섹션: 문서 안에 計上場所 → 支店 → センター → 得意先 같은 계층이 있을 때만 작성 -->

## 문서 내 계층 구조

<!-- 각 계층 헤더를 트리 구조로 표시.
     → items[].xxx 로 상속: Phase 2가 상속해야 할 필드
     — 추출 안 함: 집계·구분 헤더 (item에 포함 안 함)
     ← 항목 추출 단위: 여기서 하나의 item이 만들어짐 -->

```
계층1 (집계)          — 추출 안 함
  └ 계층2             → items[].xxx 로 상속
      └ 계층3         — 추출 안 함
          └ 得意先名  → items[].customer 로 상속
              └ 항목  ← 항목 추출 단위
```

---

## [Phase 2] 페이지 역할

| role        | 판단 기준                                |
| ----------- | ---------------------------------------- |
| `cover`   | `<!-- 판단 기준 -->`                   |
| `detail`  | `<!-- 판단 기준 -->`                   |
| `summary` | `<!-- 있으면 기재, 없으면 행 삭제 -->` |

cover 페이지 totals 키:

<!-- 키명은 청구서 원문 그대로. 필수=✓ / 선택=— -->

| 키         | 필수 | 타입       | 비고                        |
| ---------- | ---- | ---------- | --------------------------- |
| `{키명}` | ✓   | 정수       |                             |
| `{키명}` | —   | 정수·dict | `<!-- 없으면 생략 등 -->` |

---

## [Phase 2] 추출 컬럼

<!-- invoice_no / customer / product 는 모든 양식 공통 최상위 필드.
     columns{} 안에는 청구서 원문 컬럼명을 키로 사용.
     양식에 없는 행은 삭제. -->

| 필드                | 원문 필드               | 비고                              |
| ------------------- | ----------------------- | --------------------------------- |
| `invoice_no`      | {청구서 원문 컬럼명}    | `<!-- invoice_no 조합 규칙 -->` |
| `customer`        | {청구서 원문 컬럼명}    | 원문 그대로                       |
| `product`         | {청구서 원문 컬럼명}    | 원문 그대로                       |
| `columns["{키}"]` | {청구서 원문 컬럼명} 열 | `<!-- 비고 -->`                 |
| `columns["{키}"]` | {청구서 원문 컬럼명} 열 | `<!-- 비고 -->`                 |

---

<!-- ▼ 선택 섹션: OCR 특유의 오독(소수점, 콤마 오인 등) 정규화가 필요할 때만 작성 -->

## [Phase 2] OCR 정규화 규칙

<!-- 컬럼명별로 정규화 규칙을 작성. 예: ÷100 복원, 콤마 오독 처리 -->

###  —

| OCR 값            | 실제 값 | 해석 |
| ----------------- | ------- | ---- |
| `<!-- 예시 -->` |         |      |

---

<!-- ▼ 선택 섹션: 備考 열의 특정 텍스트를 applied_conditions 토큰으로 변환할 때만 작성 -->

## [Phase 2] 備考 → applied_conditions

<!-- 토큰 목록. 정의에 없는 텍스트는 무시한다. -->

| 備考 텍스트  | 토큰       |
| ------------ | ---------- |
| `{텍스트}` | `{토큰}` |

---

## [Phase 2] 함정 케이스

<!-- 집계 행 제외, 특수 행 처리, 상속 규칙 등 추출 시 주의사항.
     번호 목록으로 작성. -->

1. **{케이스명}**: 설명.
2. **{케이스명}**: 설명.

---

## [Phase 2] 크로스 페이지 감지

동일 {그룹 단위}가 여러 페이지에 걸치는 경우 `source_pages`에 양 페이지 번호를 모두 포함한다.

- **단서 1**: `<!-- 현재 페이지 끝에서 이어짐을 알 수 있는 조건 -->` → 다음 페이지로 이어짐
- **단서 2**: `<!-- 다음 페이지 시작에서 이어짐을 알 수 있는 조건 -->` → 직전 그룹의 연속

---

## [Phase 2] 출력 예시

<!-- 실제 샘플 청구서 기반 최소 예시 (2~3 item). items[]는 양식 구조를 대표하는 케이스 포함. -->

```json
{
  "pages": [
    {
      "page": 1, "role": "cover",
      "totals": {
        "{키명}": 0
      }
    },
    {"page": 2, "role": "detail"}
  ],
  "items": [
    {
      "invoice_no": "{예시}",
      "customer": "{예시}",
      "product": "{예시}",
      "columns": {
        "{키}": 0
      },
      "applied_conditions": [],
      "source_pages": [2]
    }
  ]
}
```

---

## [Phase 2] タイプ 분류 규칙

각 item의 タイプ를 아래 규칙 순서로 판단한다. 위에서 매칭되면 아래는 확인하지 않는다.

타입 9종은 모든 양식 공통 고정값. 양식마다 달라지는 것은 **조건 컬럼만**.

| 우선순위 | 조건                    | タイプ           | 제품매핑 | 비고    |
| -------- | ----------------------- | ---------------- | -------- | ------- |
| 1        |                         | `非課税`       |          |         |
| 2        |                         | `CF8%`         |          |         |
| 3        |                         | `CF10%`        |          |         |
| 4        |                         | `販促費8%`     |          |         |
| 5        |                         | `販促費10%`    |          |         |
| 6        |                         | `ロットアウト` |          |         |
| 7        |                         | `消費税8%`     |          |         |
| 8        |                         | `消費税10%`    |          |         |
| 9        | 이외 모든 항목 (기본값) | `条件`         | —       | ✅ 확정 |

조건이 비어있는 규칙은 현업 확인 후 채워넣는다. 확인 전까지는 `条件`으로 출력한다.

---

## データソース

Phase 3 소매처·판매처 매핑 시 로드할 CSV 파일 목록.

<!-- 소매처 조회 방식에 따라 선택:
     - bracket_code_csv: 괄호 코드 직접 조회 (form_01 방식)
     - domae_retail_csv: 도매소매처명→소매처코드 변환 (form_04 방식)
     항상 retail_user.csv는 포함. -->

<!-- bracket_code_csv: domae_retail_1.csv -->

<!-- domae_retail_csv: (사용 안 함 — retail_user.csv로 통합) -->

- retail_user.csv

---

## issuer 식별

cover 페이지에서 추출한 issuer 정보를 이용해 판매처를 특정할 때 사용할 필드.

```
fingerprint_fields: <!-- name / name+address 등 -->
```

<!-- 발행처 이름에 지점명·지역명이 포함되어 판매처를 구별할 수 있으면 name 만으로 충분.
     그렇지 않으면 address 등 추가 필드 지정. -->

---

<!-- ▼ 선택 섹션: 소매처코드→판매처코드가 1:N일 때 판단 기준이 있는 경우만 작성 -->

## [Phase 3] 판매처 결정 규칙

소매처코드 → 판매처코드 매핑 시 `retail_user.csv`에서 후보가 복수이면(1:N) 아래 기준으로 판단한다.

| 우선순위 | 판단 기준                                      | 처리               |
| -------- | ---------------------------------------------- | ------------------ |
| 1        | issuer.name이 판매처명과 일치 (부분 일치 포함) | 자동 확정          |
| 2        | 명확한 일치 없음                               | NEEDS_CONFIRMATION |

> 현업 확인 후 아래 테이블을 채워넣는다.

| 소매처코드          | 판매처 후보 | 판단 기준 | 확정일 |
| ------------------- | ----------- | --------- | ------ |
| (현업 확인 후 추가) |             |           |        |

---

## [Phase 4] NET 계산식

Python 코드 전용. Phase 2·3에서는 이 섹션을 무시한다.

### NET 계산

| 조건                                       | 수식                    |
| ------------------------------------------ | ----------------------- |
| `<!-- 数量単位 또는 条件タイプ 기준 -->` | `NET = 仕切 - {조건}` |

### 교차검증

| #  | 좌변 (items 집계)            | 우변 (문서 값)                | 비고                   |
| -- | ---------------------------- | ----------------------------- | ---------------------- |
| ① | `<!-- items 집계 규칙 -->` | `<!-- cover/summary 키 -->` |                        |
| ② | `<!-- items 집계 규칙 -->` | `<!-- cover/summary 키 -->` | `<!-- 생략 조건 -->` |
